{
	"name": "restflow",
	"version": "1.0.0",
	"description": "API testing flows with Restflow",
	"scripts": {
		"test": "restflow run flows/ --env .env.local",
		"test:env": "restflow run flows/ --env .env.local",
		"test:verbose": "restflow run flows/ --verbose",
		"test:json": "restflow run flows/ --format json"
	},
	"devDependencies": {
		"@restflow/cli": "workspace:*"
	}
}
