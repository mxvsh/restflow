### Get Users
GET {{BASE_URL}}/users

> assert status == 200
> assert body.users != null
> capture userCount = body.users.length

### Create User
POST {{BASE_URL}}/users
Content-Type: application/json

{
  "name": "Test User",
  "email": "test@example.com"
}

> assert status == 201
> assert body.email == "test@example.com"
> capture newUserId = body.id
